# Requirements

    Python 3.14
    fastapi
    uvicorn


# Usage
0) pip install fastapi uvicorn
1) Activate venv 
Get host's comupter IP (ipconfig)
2) Run uvicorn main:app --host 0.0.0.0 --port 8000
3) Open browser on http://192.168.0.195:8000
(Substitute with host's IP)

